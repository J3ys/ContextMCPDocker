services:
  app:
    build:
      context: .
    command: ["server"]
    env_file:
      - path: ./.env
        required: false
    environment:
      PINECONE_MODE: cloud
      PINECONE_CONTROLLER_HOST: ""
    ports:
      - "8787:8787"
    volumes:
      - ./config.cloud.yaml:/app/deployments/dodopayments/config.yaml:ro
      - ./data:/app/data

  reindex:
    build:
      context: .
    profiles: ["reindex"]
    command: ["reindex"]
    env_file:
      - path: ./.env
        required: false
    environment:
      PINECONE_MODE: cloud
      PINECONE_CONTROLLER_HOST: ""
    volumes:
      - ./config.cloud.yaml:/app/deployments/dodopayments/config.yaml:ro
      - ./data:/app/data
